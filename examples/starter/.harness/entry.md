# Project harness entry

Read the nearest manifest, refresh actual Git state, then use `specs/` as product meaning and `contracts/` as behavioral obligations. Read `.harness/git-conventions.yaml` when present before creating a branch, commit, pull request, or issue. Audit context again after compression or repeated questions. Use TDD and check semantic ownership before implementation.
